# Orchestrator: Runs documentation + marketing agents across all org repos
#
# This workflow discovers all repos in the richi-solutions org that have
# a .claude/agents/documentation.md file, then runs two Claude agents
# sequentially on each repo:
#   1. documentation — README, JSDoc, architecture docs, CONTRIBUTING
#   2. marketing     — MARKETING.md (value prop, audience, positioning)
# Changes from both agents land in one combined PR per repo.
#
# Requires org secrets:
#   - ANTHROPIC_API_KEY
#   - ORG_PAT (GitHub PAT with repo scope for creating PRs across repos)

name: Orchestrate Documentation Update

on:
  workflow_dispatch:
  # Schedule disabled — run manually via workflow_dispatch to save ~50 EUR/month
  # Previously: cron: "0 5 * * 1" (Weekly Monday 05:00 UTC)

jobs:
  discover:
    name: Discover Repos
    runs-on: ubuntu-latest
    outputs:
      repos: ${{ steps.list.outputs.repos }}
    steps:
      - name: List org repos with documentation agent
        id: list
        env:
          GH_TOKEN: ${{ secrets.ORG_PAT }}
        run: |
          # Get all non-archived repos in the org
          REPOS=$(gh repo list richi-solutions \
            --no-archived \
            --json name,name \
            --limit 100 \
            -q '.[].name' \
            | grep -v '^orchestrator\.richi\.solutions$' \
            | grep -v '^\\.claude$')

          # Filter to repos that have the documentation agent
          VALID_REPOS="[]"
          for repo in $REPOS; do
            if gh api "repos/richi-solutions/$repo/contents/.claude/agents/documentation.md" --silent 2>/dev/null; then
              VALID_REPOS=$(echo "$VALID_REPOS" | jq --arg r "$repo" '. + [$r]')
            fi
          done

          echo "repos=$(echo $VALID_REPOS | jq -c .)" >> "$GITHUB_OUTPUT"
          echo "Found repos with documentation agent: $VALID_REPOS"

  update-docs:
    name: Update Docs — ${{ matrix.repo }}
    needs: discover
    if: needs.discover.outputs.repos != '[]'
    runs-on: ubuntu-latest
    strategy:
      matrix:
        repo: ${{ fromJson(needs.discover.outputs.repos) }}
      fail-fast: false
      max-parallel: 3
    permissions:
      contents: write
      pull-requests: write

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
        with:
          repository: richi-solutions/${{ matrix.repo }}
          token: ${{ secrets.ORG_PAT }}

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "22"

      - name: Install Claude Code
        run: npm install -g @anthropic-ai/claude-code

      - name: Run Claude Documentation Agent
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
        run: |
          claude -p "You are the documentation agent. Read .claude/agents/documentation.md for your full instructions, then analyze this project and generate/update all documentation: README.md, code comments (JSDoc/@fileoverview), architecture docs, CONTRIBUTING.md, and .env.example. CRITICAL RULES: Do NOT modify application logic. Do NOT modify any files under .claude/ — that directory is managed by sync-dotclaude and must not be touched. Do NOT modify MARKETING.md — the marketing agent owns that file." \
            --allowedTools "Bash,Read,Edit,Write,Grep,Glob" \
            --model claude-sonnet-4-6 \
            --max-turns 30 \
            --output-format json \
            > /tmp/docs-result.json 2>&1 || true

      - name: Run Claude Marketing Agent
        if: hashFiles('.claude/agents/marketing.md') != ''
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
        run: |
          claude -p "You are the marketing agent. Read .claude/agents/marketing.md for your full instructions, then analyze this project and generate/update MARKETING.md only. CRITICAL RULES: Only modify MARKETING.md. Do NOT modify README.md, CONTRIBUTING.md, source code, tests, or any files under .claude/. Do NOT invent features that are not in the codebase." \
            --allowedTools "Bash,Read,Edit,Write,Grep,Glob" \
            --model claude-sonnet-4-6 \
            --max-turns 20 \
            --output-format json \
            > /tmp/marketing-result.json 2>&1 || true

      - name: Create Docs PR if changes exist
        env:
          GH_TOKEN: ${{ secrets.ORG_PAT }}
        run: |
          if [ -n "$(git status --porcelain)" ]; then
            BRANCH="docs-update-$(date +%Y%m%d)"

            # Check if branch already exists (avoid duplicate PRs)
            if git ls-remote --heads origin "$BRANCH" | grep -q "$BRANCH"; then
              echo "Branch $BRANCH already exists for ${{ matrix.repo }}, skipping"
              exit 0
            fi

            # Revert any changes to .claude/ (managed by sync-dotclaude)
            git checkout -- .claude/ 2>/dev/null || true

            git config user.name "claude-docs-bot"
            git config user.email "docs@richi-solutions.com"
            git checkout -b "$BRANCH"
            git add -A -- ':!.claude/'
            git commit -m "$(cat <<'EOF'
          docs: automated documentation + marketing update

          Generated/updated by Claude documentation and marketing agents:
          - README.md improvements
          - JSDoc comments on exported functions
          - @fileoverview headers on modules
          - Architecture docs and contributing guide
          - MARKETING.md value proposition and positioning copy

          Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>
          EOF
            )"
            git push origin "$BRANCH"

            # Extract summaries from both agent outputs
            DOCS_SUMMARY=$(cat /tmp/docs-result.json | jq -r '.result // "No summary available"' 2>/dev/null || echo "Documentation update completed, see diff for changes.")
            MARKETING_SUMMARY=$(cat /tmp/marketing-result.json 2>/dev/null | jq -r '.result // "Marketing agent did not run"' 2>/dev/null || echo "Marketing agent did not run")
            SUMMARY=$(printf '### Documentation\n\n%s\n\n### Marketing\n\n%s' "$DOCS_SUMMARY" "$MARKETING_SUMMARY")

            # Ensure labels exist (create if missing)
            gh label create "documentation" --repo "richi-solutions/${{ matrix.repo }}" --color "0075ca" --description "Documentation updates" 2>/dev/null || true
            gh label create "marketing" --repo "richi-solutions/${{ matrix.repo }}" --color "e99695" --description "Marketing copy updates" 2>/dev/null || true
            gh label create "automated" --repo "richi-solutions/${{ matrix.repo }}" --color "bfdadc" --description "Automated by CI" 2>/dev/null || true

            gh pr create \
              --repo "richi-solutions/${{ matrix.repo }}" \
              --title "docs: automated documentation + marketing update $(date +%Y-%m-%d)" \
              --body "$(cat <<EOF
          ## Documentation + Marketing Update — Automated

          This PR was generated by the Claude documentation and marketing agents.

          ### What Changed

          $SUMMARY

          ### Review Checklist

          - [ ] README.md changes are accurate
          - [ ] Code comments are correct and helpful
          - [ ] MARKETING.md positioning matches actual product capabilities
          - [ ] No application logic was modified
          - [ ] File paths and script names are verified

          ---
          _Generated by Claude documentation + marketing agents via [orchestrate-docs.yml](https://github.com/richi-solutions/orchestrator.richi.solutions)_
          EOF
            )" \
              --label "documentation,marketing,automated" \
              2>&1 | tee /tmp/pr-url.txt

            # Auto-merge the PR (squash) to minimize branch lifetime
            PR_URL=$(grep -o 'https://[^ ]*' /tmp/pr-url.txt || true)
            if [ -n "$PR_URL" ]; then
              gh pr merge "$PR_URL" --squash --delete-branch --admin || echo "Auto-merge failed for ${{ matrix.repo }}, PR remains open for manual review"
            fi
          else
            echo "No documentation or marketing changes needed for ${{ matrix.repo }}"
          fi
